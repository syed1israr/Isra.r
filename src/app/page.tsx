import { Button } from "@/components/ui/button";

export default function Home() {
  return(
    <Button variant="secondary">
      <span className="text-white">Destructive Button</span>
    </Button>
  );
};
